{
  "buildCommand": "cd solar-system && npm install && npm run build",
  "outputDirectory": ".",
  "routes": [
    { "src": "/solar-system/(.*)", "dest": "/solar-system/dist/$1" },
    { "src": "/", "dest": "/index.html" }
  ]
}
